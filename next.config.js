/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // trailingSlash: true,
  // images: {
  //   loader: "akamai",
  //   path: "/",
  // },
  // output: "export", // 정적 HTML 파일로 변환
};

module.exports = nextConfig;
